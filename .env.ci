# CI Environment Configuration
# 
# This file defines environment variables specifically for CI/CD environments.
# These settings ensure consistent behavior across different CI platforms.

# =============================================================================
# CI IDENTIFICATION
# =============================================================================

# CI environment marker (automatically set by CI platforms)
CI=true

# =============================================================================
# TEST STRATEGY - ALWAYS USE MOCKS IN CI
# =============================================================================

# Force mocks in CI environment for reliability and speed
VITE_TEST_USE_MOCKS=true

# Disable local database testing in CI
VITE_TEST_USE_LOCAL_DB=false

# Enable debug logging in CI for troubleshooting
VITE_VITEST_DEBUG=true

# =============================================================================
# SUPABASE CONFIGURATION - MOCK VALUES
# =============================================================================

# These are placeholder values since we use mocks in CI
# They prevent configuration errors but are not actually used
VITE_SUPABASE_URL=https://mock-project.supabase.co
VITE_SUPABASE_ANON_KEY=mock-anon-key-for-ci-testing

# =============================================================================
# CI-SPECIFIC OPTIMIZATIONS
# =============================================================================

# Node environment
NODE_ENV=test

# Disable unnecessary services in CI
VITE_ANALYTICS_ENABLED=false
VITE_TELEMETRY_ENABLED=false
VITE_ERROR_REPORTING=false

# Test performance optimizations
VITE_TEST_TIMEOUT=10000
VITE_TEST_PARALLEL=true

# =============================================================================
# SECURITY AND PRIVACY
# =============================================================================

# Disable data collection in CI
VITE_COLLECT_METRICS=false
VITE_SEND_TELEMETRY=false

# Mock external service integrations
VITE_TEST_MOCK_EXTERNAL_APIS=true
VITE_TEST_SKIP_AUTH=true
VITE_TEST_SKIP_NETWORK=true

# =============================================================================
# TELEMETRY CONFIGURATION - DISABLED IN CI
# =============================================================================

# Telemetry disabled in CI
VITE_VERCEL_TELEMETRY_ENABLED=false
VITE_SUPABASE_TELEMETRY_ENABLED=false

# =============================================================================
# CI PLATFORM SPECIFIC
# =============================================================================

# GitHub Actions specific settings
# GITHUB_ACTIONS=true (set automatically)
# RUNNER_OS=Linux (set automatically)

# =============================================================================
# NOTES
# =============================================================================
#
# This configuration ensures that:
# 1. All tests use mocks for consistency and speed
# 2. No external dependencies are required
# 3. Tests are deterministic and reliable
# 4. Security is maintained with no real credentials
# 5. Performance is optimized for CI environments
#
# The dual strategy implementation automatically detects
# CI environments and switches to mock mode regardless
# of other environment variable settings.
#